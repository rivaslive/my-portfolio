import styled from 'styled-components';
import Container from 'components/Atoms/Container';

export const StyleWrapper = styled(Container)`
  height: 450px;
  border-radius: 12px;
  margin-bottom: 130px;

  svg {
    filter: drop-shadow(0 20px 53px #075dc0);
  }
`;
